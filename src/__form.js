/**
 * 1. use form tag and onSubmit handler with event (e) from e access e.target.name.value
 
 * 2.Controlled Element: use indivisual flied state for each input field
 * 3.Controlled Element: one object holding all input fileds values
 * 4.Uncontrolled Element: useRef to create a reference to the element and access value by using like: nameRef.current.value
 */